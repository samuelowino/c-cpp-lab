//Section 1: Namespaces

/**
 * A namespace is a mechanism for expressing logical grouping.
 * That is,
 * if some declarations logically belong together according to some criteria,
 * they can be put in a common namespace to express that fact.
 *
 **/
namespace Parser {
     double expr(bool);
     double prim(bool);
     double term(bool);
 }

//Definition
double Parser::prim(bool get){
    /** Definition of prim() **/
}

double Parser::expr(bool get){
    /** Definition of expr() **/
}

double Parser::term(bool get){
    /** Definition of term() **/
}


