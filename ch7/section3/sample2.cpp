//Section 3: Arguments; Unspecified Arguments (...)
#include <iostream>

using namespace std;

int main(){
    /**
     * For some functions, it is not possible to specify
     * the number and type of all arguments expected in a call.
     * Such a function is declare by terminating the list 
     * of argument declarations with the ellipsis (...),
     * which means
     *      "and maybe some more arguments."
     *
     **/
    int printf(const char*);
}
